# Todo Queue

## 当前优先级 P0

### P0-1 修复武器判定框死角问题
状态：已修复，待实机验证。

### P0-2 修复角色转向后武器方向异常
状态：已处理，需实机复测。

---

## 已完成

### 像素角色模型包与类型目录
状态：✅ 第一版完成 (2026-06-22)。

详情：
- 角色模型资产按类型分文件夹：主角、伙伴、墨境 NPC、办公室 NPC、普通怪物、Boss
- 新增模型包索引和生产说明，后续替换模型时优先维护 `model_pack_index.json` 和每个模型自己的 `config.json`
- 主角已有 rig-baked jump、turnaround、分帧、strip 和锚点预览
- 18 个非主角模型已有第一版 `idle/walk/talk/hurt` 动作包和 `motion_profile.json`
- 办公室 NPC 激活态接入 `talk_strip.png`，不再只回退 idle
- 验证通过：80 个 JS 语法检查、19 个模型资源检查、83 个 HTTP 模块递归加载、浏览器 Runtime 检查

后续建议：
- 优先把办公室四人组、Supply Keeper、Inkdot、常见怪物各挑一个升级为主角同级的 rig-baked 动作
- 给每个重要 NPC 补专属工作/对话/受压动作，而不是只用通用 whole-sprite transform

### 探索欲第一版：未知地图与可发现物
状态：✅ 第一版完成 (2026-06-20)。

详情：
- 全图不再直接暴露所有房间；未探索房间隐藏真实类型，只在接近已探索区域时显示未知问号
- 主墨境新增可检查探索点：Margin note、Breathing ink、Folded receipt、Wrong shadow、Warm stain
- 探索点会刷在探索/事件/资源/危险/裂口/宝箱等房间，靠近按 E inspect
- 记录发现会给 Flow、材料、墨水、身体恢复或稀有发现，并在 HUD 显示 finds 进度
- 早晨反馈页新增 Exploration finds，让探索结果进入日终记忆
- 外置 tuning.config.js 新增 exploration 调参段，可调探索点数量、生成概率、检查距离和奖励

### 短循环爽点：Flow / Ink Rush
状态：✅ 第一版完成 (2026-06-20)。

详情：
- 主墨境新增 Flow 节奏条：进入新房间、击杀敌人、清房、开宝箱、画布门返回、裂口返回都会涨 Flow
- Flow 满格触发 Ink Rush，短时间提升移动、攻击伤害、墨水恢复，并降低冲刺成本
- HUD 左上角新增 Flow / Ink Rush 条和即时反馈文案，让玩家知道自己正在连贯推进
- Flow 参数已放进外置 StandalonePrototype/tuning.config.js，可调涨条速度、Rush 时长和强化倍率
- 目标：先补“这一分钟好不好玩”的短反馈，不再只靠长期结算和系统堆叠

### 白天场景入口第一版
状态：✅ 第一版完成 (2026-06-20)。

详情：
- Studio 新增 `DAY ROUTES` 面板：`S` 进入商店，`C` 进入公司，`E` 回到画板
- Work Phase 从文字事件页改成 Zhou Studio 像素办公室，保留现有工作事件和状态影响
- Shop 从纯购买列表改成 `INKDOT NOOK` 小店，有货架、柜台、店主和商品详情面板
- 当前仍是“场景骨架第一版”，后续需要补公司 NPC 对话、通勤/街区、商店库存随夜晚发现变化

### 集中调参表
状态：✅ 已完成 (2026-06-20)。

详情：
- 新增 `StandalonePrototype/src/core/TuningConfig.js`，集中管理地图尺寸、夜晚时长、地图层房间数、地图规则权重和画布门返回传送概率
- `WORLD_W` / `WORLD_H`、夜晚时间、浅/中/深层 yRange、房间数量、地图规则权重已从调参表读取
- 画布门探索完成后按 `TUNING.portalReturn` 概率随机回流到主地图其他房间
- 修复 `StoryNPC.js` 的 `promptPulse` 作用域错误
- 恢复被空字节污染的主场景/墨境绘制相关文件，并重新接回必要版本 import
- 验证：`node --check`、8 次地图生成 reachable、递归 import 扫描 71 个模块、主场景 `start/update/draw` 烟测通过

### Canvas Rift 画稿裂口系统集成
状态：已完成。

内容：
- 5 种裂口类型（温馨/废稿/AI污染/记忆/未知作者），5 种视觉预设
- 3 层深度地图（浅层10%/中层20%/深层35% 裂口概率）
- RiftWorld 子世界（5 种独立场景 + 嵌套裂口支持）
- 出口随机化、深度 HUD、图鉴记录
- 修复 MapLayers.js 缺失导致的导入报错

### P0-1 / P0-2 状态
- P0-1：swept melee 命中检测加入 NPC 体积补偿
- P0-2：swept melee 统一使用 attackAngle 方向

### P1-1 增强不同武器的攻击动画差异
状态：✅ 已完成 (2026-06-16)。

详情：
- 新增 `daggerSlash`（匕首连击 + flick 震荡）、`whipLash`（鞭打蓄力+爆裂+波浪）、`staffSpin`（法杖 210° 旋转横扫）三种专属动画
- 修改文件：`AttackController.js`、`WeaponSpriteRenderer.js`、`WeaponArchetypes.js`、`builds/WeaponArchetypes.js`

### P1-3 优化地图生成玩法节奏
状态：✅ 第一版完成 (2026-06-18)。

详情：
- 主地图改成纵向“下潜”结构：蜿蜒主下潜通道 + 左右分支洞穴
- 浅/中/深三层有更明确的资源、补给、危险、裂口分布
- 深层保证出现更高风险房间和裂口房，形成风险收益推进
- 全图 UI 增加深度带、房间图标、主路/分支和高风险标记
- 新增 4 种地图生成规则：墨脊下潜、双流裂谷、碎礁群洞、塌陷墨井
- 地图规则会改变竖井数量、支路密度、房间分布和危险偏向
- HUD 左上角和全图 UI 会显示当前地图规则

### 墨迹移动第一版
状态：✅ 第一版完成 (2026-06-18)。

详情：
- Shift 改为笔触闪掠：支持 WASD/方向键的斜向、向上、向下移动
- 空中按住 Space 会进入墨漂浮，降低下落速度并消耗少量墨水
- 贴墙按方向会墨迹缓降，按 Space 可从墙面反弹跳出
- 增加移动残影、漂浮曲线和墙面墨迹反馈

### 像素风视觉第一版
状态：✅ 第一版完成 (2026-06-19)。

详情：
- 墨境背景改成分层像素洞穴/深海感，加入块状远景、水柱、漂浮像素尘
- 地块改成高对比像素描边、顶面高光、侧面暗边和纸/石墨/墨块纹理
- 主角 sprite 增加更明确的像素轮廓、亮面、墨罐装备和青蓝高光
- 画布门从 emoji/柔光改成像素画框、门芯扫描线和像素火花
- 画布门/裂口子世界的收集物、生物、出口和玩家统一为像素方块风
- 主墨境加世界层像素水体/暗角滤镜，HUD 保持清晰
- 新增 `VisualThemes.js`，浅/中/深三层各自有背景色带、地块色、HUD 强调色和氛围颗粒
- 敌人、Boss、宝箱、材料和道具增加黑底像素轮廓，复杂地形中更容易读
- 裂口入口增加像素背板、中心核心和标签底条，入口语义更明确
- 新增 `LayerLandmarks.js`，主墨境房间内出现层级化像素地标和短标签
- 玩家进入浅/中/深新层级时显示短标题，强化下潜节奏
- 新增 `StudioRoom.js`，画板操作页改为可走动的夜晚房间/桌面环境
- studio 默认是 room 状态，玩家需靠近画板按 E 或点击画板进入 drawing 状态
- 画板笔触改为方块采样像素笔触，导出的武器图同步像素化
- 武器识别和结果面板改为像素读数屏，减少普通工具页感

### 传送入口第一轮打磨
状态：✅ 第一版完成 (2026-06-18)。

详情：
- 画布门不再刷到入口、返回口、Boss、裂口和最高压危险房
- 画布门记录所在房间、层级和风险信息，方便 UI 与奖励联动
- 主地图靠近画布门/裂口时显示类型、危险等级和奖励倾向
- 进入/返回入口时显示屏幕反馈，不再只写 console
- 修复 RiftWorld 暴露的 worldState 不是实时对象的问题

### 画布门内容生成升级
状态：✅ 第一版完成 (2026-06-18)。

详情：
- 每次进入画布门会抽取一个子世界规则主题，不再只是固定小房间
- 子世界被拆成探索、事件、追逐、战斗、异常、危险、奖励等特殊房间
- 玩家进入房间时显示房间类型、房间名和短目标，HUD 同步当前房间
- 温馨涂鸦增加会跑的太阳、蜡笔河、纸船愿望等童年回忆内容
- 恐怖草图增加橡皮擦巡逻、门缝凝视、描黑线等时间压力机制
- AI 模板增加完美网格、复制扫描线、提示词墓碑和异常像素
- 战斗房支持敌性生物接触扣时间、F 驱散负面/模板生物和敌人血条
- 子地图 HUD 会显示当前规则、房间名、委托进度、发现数量和出口开启条件
- 画布门内部新增门内委托，出口由委托完成度开启，不再只是收集够数量
- 非战斗房新增可见目标节点，E/R/F 分别对应调查、记录、强行处理
- 奖励房新增稳妥奖励 / 冒险加码选择，冒险会触发额外处理目标
- 温馨涂鸦、恐怖草图、AI 模板的目标处理方式开始分化
- AI 模板目标会显示当前解码键，按错会扣时间并可能激活额外扫描线
- 新增 `DiscoveryEffects.js`，画布门发现物返回主地图后触发灌注效果
- 温暖回响恢复生命/墨水并增加武器触及；恐惧锋口增加伤害/暴击但扣生命；越界模板增加攻速/触及但提高不稳定性
- `inkwell.js` 返回主地图时显示画作完整度和灌注结果
- `main.js` 早晨结算页新增 Discovery infusion 行
- 修复 `ArtworkCompletion.js` 的 `jackpotsHit` 计数拼写错误
- 新增 `NightObjectives.js`，每晚进入墨境会抽取 Tonight goal
- HUD 右侧显示目标进度；完成目标后给小奖励并弹出提示
- 早晨结算页新增 Night goal 结果行
- `bootstrap.js` / `main.js` 已 bump 模块版本号，避免浏览器缓存旧逻辑
- 新增 `PixelPolish.js`，主墨境加入前景像素遮挡、暗角抖动、扫描线和边缘颗粒
- `Background.js` 新增远景墙面像素块、裂缝感和像素光束
- `TileDrawing.js` 新增地块碎屑、裂纹和深层发光微细节
- `inkwell.js` 接入 PixelPolish 前景/后处理层，UI 保持在润色层之上
- 新增 `GateConsequences.js`，画布门内选择会转成主地图后果
- `SubMapScene.js` 结算会返回 outcome：稳妥/冒险/强行处理/模板误触/危险拆除都会被记录
- `inkwell.js` 新增 canvas pressure 和 depth pressure；深潜和画布门压力会消耗墨水，高压低墨会伤身体
- 完成画布门后会留下主地图像素回声，稳定类像捷径，追猎类像污染脉冲
- 高风险画布门后果会通过 `spawnGateHunters()` 在主地图刷出追猎敌人
- 画布门返回时有概率随机传送到主地图其他房间；高风险后果更容易甩到危险/深层/支路房
- `main.js` 早晨结算页新增 Canvas consequence 行，记录门内选择造成的夜晚后果
- 新增外置调参表 `StandalonePrototype/tuning.config.js`；本地服务运行时改数值并刷新浏览器即可测试，不需要改源码或重启服务

### OpeningScene 开场场景
状态：已完成。

### Chapter0 序章场景
状态：已完成，待实机验证。

### 项目梳理阶段 — 设计文档
状态：已完成。

---

## 当前优先级 P1

### P1-2 完善画板类型到武器系统的消费链路
目标：画出来的类型必须实际进入战斗系统，而不是只在 UI 层显示。

### P1-4 完善裂口子世界生物种类和奖励平衡
目标：每种裂口子世界增加更多独特生物，调优 spawn 概率和奖励曲线。

### P1-5 裂口发现物 → 画作完整度结算链路
目标：从裂口带回的发现物能提升 artworkCompletion。
状态：画布门发现物链路已先行完成；裂口 RiftWorld 仍待接入同类结算。

---

## 当前优先级 P2（后续）

### P2-1 Day Cycle "work" 阶段玩法
目前仅有 phase 定义，无场景/玩法。

### P2-4 长期章节目标板块
目标：在每日 Tonight goal 之上，补一条跨多天的章节目标线，让玩家知道为什么持续下潜和修复画作。

### P2-5 精致像素表现继续打磨
目标：补攻击命中特效、门内房间专属像素资产、敌人受击反馈和更清晰的层级色彩校准。

### P2-2 Chapter0 实机验证与微调
- 调查物品交互距离
- 中文文本渲染
- 墨迹扩散过渡

### P2-3 实机验证 P0 两个 bug 修复
使用浏览器逐一测试 4 种武器在各方向攻击时的 hitbox 表现。
